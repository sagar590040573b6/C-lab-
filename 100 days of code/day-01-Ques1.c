#include<stdio.h>
int main() {
    
int n;
printf("Enter the value of n: ");
scanf("%d", &n);

int m;
printf("Enter the value of m: ");
scanf("%d", &m);

int sum = n + m;
printf("sum is %d\n", sum);

    return 0;
}
